import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";

const geist = Geist({
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "RAW ENGINE | Creative Poster Design Studio",
  description:
    "Premium poster designs, branding and social media creatives by RAW ENGINE.",
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geist.className} bg-black text-white`}>
        {children}
      </body>
    </html>
  );
}